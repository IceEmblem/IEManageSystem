interface PageComponentBaseSettingModel {
    sortIndex: number,
    col: string | null,
    height: string | null,
    padding: string | null,
    margin: string | null,
    backgroundColor: string | null,
    className: string | null,
}